name: Makefile CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v1

    - name: Install poetry
      run: |
        pip install -U pip
        pip install poetry

    - name: Install dependencies
      run: make init
