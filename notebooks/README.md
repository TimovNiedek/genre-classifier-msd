# Notebooks

## Getting started

Initialize a pipenv environment with the required dependencies:

```bash
pipenv install
```

Start a Jupyter notebook server:

```bash
pipenv run jupyter notebook
```
